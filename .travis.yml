language: node_js
node_js:
  - 'stable'
install:
  - yarn
  - yarn add coveralls
script: 'yarn test && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js'
notifications:
  email: false
