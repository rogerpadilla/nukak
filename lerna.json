{
  "$schema": "https://lerna.js.org/schemas/2.0.0/lerna-schema.json",
  "version": "independent",
  "command": {
    "publish": {
      "conventionalCommits": true,
      "message": "chore(release): publish",
      "createChangelog": true,
      "allowBranch": "uql-unification",
      "ignoreChanges": ["**/*.spec.ts", "**/*.it.ts", "**/test/**"],
      "forcePublish": true,
      "push": false,
      "gitRemote": "origin"
    }
  },
  "packages": ["packages/*"]
}
